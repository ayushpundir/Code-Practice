#include <stdio.h>
#include <math.h>

int main(){
    int side;
    printf("Enter side a:");
    scanf("%d", &side);
    printf("The area of square is %f\n", pow(side,2));//pow double,,,
    return 0;// ,,,value return karta ha
}